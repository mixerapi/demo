openapi: "3.0.0"
info:
  version: "v0.0.0"
  title: Authentication API
  description: |

  license:
    name: MIT

paths:


components:
  schemas:
    Exception:
      type: object
      properties:
        code:
          type: integer
          example: 500
        url:
          type: string
          example: /url/path
        message:
          type: string
          example: Internal Error
      xml:
        name: response
